[project]
name = "ssh-wrapper"
version = "0.1.0"
description = "SSH wrapper dependencies for REF"
requires-python = ">=3.10"
dependencies = [
    "colorama",
    "itsdangerous",
    "pip-chill",
    "requests",
]
